packages:
  - "."

# Packages whose postinstall/build scripts should be silently skipped.
# (These either don't need them or provide prebuilt binaries.)
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
